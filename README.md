### Note 
Most coding done in Jupyter notebooks as is my preference. The code will look like rubbish unless you pull it locally and have VSCode's Jupyter extension installed. Transferring to a .py file is easy enough, just ask.

### Projects published here:
1. DMS parameter list generator; provide a list of VMVs and receive the unique parameter list values assigned to the set. Note that the reference files should be up to date; i.e. if any new parameter types are added to the DMS, these files should be updated. See mappings subfolder.